meta {
  name: Update Post
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/posts/{{postId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "title": "Updated Title",
    "visibility": "public"
  }
}
